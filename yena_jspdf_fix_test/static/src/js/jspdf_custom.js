odoo.define('jspdf_fix.jspdf_custom', function (require) {
    "use strict";

    var core = require('web.core');

    // Burada jspdf kütüphanesini düzeltmek için gerekli olan kodu yazın.
    // Örneğin, kütüphaneyi yeniden başlatma veya yeni bir versiyonunu yüklemek gibi.
    console.log('JSPDF custom fix is applied');
});
